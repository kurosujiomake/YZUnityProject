using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponSwaps : MonoBehaviour
{
    public GameObject[] weps; //the up to 4 weapons the player can carry
    public GameObject activeWep = null; //the mainhand wep currently active and is used in the att animations

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
